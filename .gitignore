.venv/
__pycache__/
*.pyc
.env
.env.*
!.env.example
*.log

# Local state when running from source
.claude-web/
workspace/
claude-home/
claude-homes/
claude-web-state/
codex-home/
codex-homes/

# PyInstaller build output (claude_web.spec). Native bundles for each
# platform are produced in CI on tag push and attached to the release;
# the local build/ and dist/ trees are noise.
build/
dist/
