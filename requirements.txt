claude-agent-sdk>=0.2.152
fastapi>=0.115
uvicorn[standard]>=0.30
jinja2>=3.1
python-multipart>=0.0.20
authlib>=1.3
itsdangerous>=2.2
httpx>=0.27

# Roundtable multi-AI debate package (vendored under ./roundtable/).
# API keys are read from env at runtime (GEMINI_API_KEY, OPENAI_API_KEY,
# ANTHROPIC_API_KEY) and are NEVER committed to source.
anthropic>=0.39
google-genai>=0.5
openai>=1.50
# Floor matches claude-agent-sdk 0.2.82+ for CVE-2025-66416.
mcp>=1.23.0
# Local schema validation for roundtable_ask_structured (parse + repair loop).
jsonschema>=4.20
# Headless browser used by auto_signin.py to drive Anthropic's magic-link
# sign-in end-to-end. After `pip install -r requirements.txt`, run
# `playwright install chromium` (or `--with-deps` on Linux) to fetch the
# browser binary. Auto-signin is off unless CLAUDE_WEB_MAILBOX_POLL_CMD is
# set on the host, so this dep is present-but-idle for installs that don't
# want it.
playwright>=1.55
